{
  "name": "pwa-wrapper",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tunnel": "./node_modules/.bin/vk-tunnel --insecure=1 --http-protocol=http --ws-protocol=wss --host=127.0.0.1 --port=80 app_id=51666014 endpoints=[web]"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^1.0.1",
    "@vkontakte/vk-tunnel": "^0.1.3",
    "typescript": "^5.0.2",
    "vite": "^4.3.9"
  },
  "dependencies": {
    "@vkontakte/superappkit": "^1.60.0"
  },
  "resolutions": {
    "es5-ext": "0.10.50"
  }
}